@inject NavigationManager NavigationManager

<button class="back-to-home" @onclick="GoHome" aria-label="Go back to home">
    ← Home
</button>

@code {
    private void GoHome()
    {
        NavigationManager.NavigateTo("/");
    }
}
